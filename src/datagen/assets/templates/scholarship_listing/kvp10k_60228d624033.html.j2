<style>
  @page {
    size: A4;
    margin: 0;
  }
  .page {
    width: 794px;
    height: 1123px;
    padding: 50px 60px;
    box-sizing: border-box;
    overflow: hidden;
    page-break-after: always;
    position: relative;
    font-family: "DejaVu Serif", "Liberation Serif", Georgia, "Helvetica", Arial, "DejaVu Sans", "Liberation Sans";
  }
  .page:last-child {
    page-break-after: auto;
  }
  h1, h2, h3 {
    font-weight: bold;
  }
  .divider {
    border-top: 1px solid #ccc;
    margin: 20px 0;
  }
</style>

<div class="page">
  <p>FCGR Research Military/Veteran Scholarships 4/2022</p>

  <h2>SCHOLARSHIPS FOR VETERANS AND MILITARY ACTIVE DUTY</h2>

  <p><strong>VFW Support Clips Help A Hero Scholarship /Student Veteran Support Grants / VFW Service Officers</strong> <a href="{{ vfw_url }}">{{ vfw_url }}</a></p>
  <p>
    <span data-field="vfw_address">{{ vfw_address }}</span> Tel: <span data-field="vfw_phone">{{ vfw_phone }}</span> SA Chapter Email: <span data-field="vfw_email">{{ vfw_email }}</span>
  </p>
  <p>Scholarships of up to <span data-field="vfw_amount_max">{{ vfw_amount_max }}</span> Veterans – Military Active Duty, Reservists – National Guard</p>

  <p><strong>Pat Tillman Foundation</strong> <a href="{{ pt_tillman_url }}">{{ pt_tillman_url }}</a></p>
  <p>
    <span data-field="pt_tillman_address">{{ pt_tillman_address }}</span>
  </p>
  <p>Scholarship Amount: Averages <span data-field="pt_tillman_avg_amount">{{ pt_tillman_avg_amount }}</span> Active duty members – Veterans – Military spouses</p>

  <p><strong>Army Nurse Corps Association Scholarship</strong> <a href="{{ army_nurse_url }}">{{ army_nurse_url }}</a></p>
  <p>
    <span data-field="army_nurse_address">{{ army_nurse_address }}</span> Tel: <span data-field="army_nurse_phone">{{ army_nurse_phone }}</span> or <span data-field="army_nurse_phone_alt">{{ army_nurse_phone_alt }}</span>
  </p>
  <p>Scholarship Amount: <span data-field="army_nurse_amount">{{ army_nurse_amount }}</span> Military Active Duty – Veterans (Army, Army National Guard, Army Reserve)</p>

  <p><strong>NBCC Foundation Military Scholarship</strong> <a href="{{ nbcc_url }}">{{ nbcc_url }}</a></p>
  <p>
    <span data-field="nbcc_address">{{ nbcc_address }}</span> Tel: <span data-field="nbcc_phone">{{ nbcc_phone }}</span> Email: <span data-field="nbcc_email">{{ nbcc_email }}</span>
  </p>
  <p>Scholarship Amount: Up to <span data-field="nbcc_amount">{{ nbcc_amount }}</span> Military Active Duty – Veterans – Spouses (pursuing a career in professional counseling)</p>

  <p><strong>AFCEA Educational Foundation / AFCEA War Veterans Scholarship</strong> <a href="{{ afcea_url }}">{{ afcea_url }}</a></p>
  <p>
    <span data-field="afcea_address">{{ afcea_address }}</span> Contact: <span data-field="afcea_phone">{{ afcea_phone }}</span>
  </p>
  <p>Military Active Duty – Veterans – Reservists – National Guard</p>

  <p><strong>AMVETS Scholarship</strong> <a href="{{ amvets_url }}">{{ amvets_url }}</a></p>
  <p>
    <span data-field="amvets_address">{{ amvets_address }}</span> Tel: <span data-field="amvets_phone">{{ amvets_phone }}</span> Email: <span data-field="amvets_email">{{ amvets_email }}</span> / programs@amvets.org
  </p>
  <p>Scholarship Amount: <span data-field="amvets_amount">{{ amvets_amount }}</span> Veterans or Active Duty – Spouse – Children/Grandchildren</p>

  <p><strong>Fleet Reserve Association Education Foundation Scholarship</strong> <a href="{{ fleet_reserve_url }}">{{ fleet_reserve_url }}</a></p>
  <p>
    <span data-field="fleet_reserve_address">{{ fleet_reserve_address }}</span> Tel: <span data-field="fleet_reserve_phone">{{ fleet_reserve_phone }}</span> Email: <span data-field="fleet_reserve_email">{{ fleet_reserve_email }}</span>
  </p>
  <p>Scholarship Amount: <span data-field="fleet_reserve_amount">{{ fleet_reserve_amount }}</span> Veterans – VSCO: <span data-field="fleet_reserve_ext">{{ fleet_reserve_ext }}</span></p>
  <p>Navy, Marine Corps, and Coast Guard</p>

  <div class="divider"></div>

  <p><strong>Army Women's Foundation Legacy Scholarship</strong> <a href="{{ army_women_url }}">{{ army_women_url }}</a></p>
  <p>
    <span data-field="army_women_address">{{ army_women_address }}</span> Tel: <span data-field="army_women_phone">{{ army_women_phone }}</span> Email: <span data-field="army_women_email">{{ army_women_email }}</span>
  </p>
  <p>Scholarship Amount: <span data-field="army_women_amount">{{ army_women_amount }}</span></p>
  <p>Women Soldiers, past and present, and their lineal descendants to help them attain their educational goals.</p>

  <p><strong>Imagine America's Military Award Program</strong> <a href="{{ imagine_america_url }}">{{ imagine_america_url }}</a></p>
  <p>
    <span data-field="imagine_america_address">{{ imagine_america_address }}</span> Tel: <span data-field="imagine_america_phone">{{ imagine_america_phone }}</span> Email: <span data-field="imagine_america_email">{{ imagine_america_email }}</span>
  </p>
  <p>Scholarship Amount: <span data-field="imagine_america_amount">{{ imagine_america_amount }}</span> Military Service Members Transitioning To Civilian Life</p>

  <p><strong>Wings Over America Scholarship</strong> <a href="{{ wings_over_america_url }}">{{ wings_over_america_url }}</a></p>
  <p>
    <span data-field="wings_over_america_address">{{ wings_over_america_address }}</span> Tel: <span data-field="wings_over_america_phone">{{ wings_over_america_phone }}</span>
  </p>
  <p>Scholarship Amount: Average <span data-field="wings_over_america_amount">{{ wings_over_america_amount }}</span> Navy Veterans – Spouses – Children; as well as active-duty</p>

  <div class="divider"></div>

  <p><strong>Combat NBC Universities Scholarship with Student Veteran Association</strong></p>
  <p>
    <span data-field="combat_nbc_address">{{ combat_nbc_address }}</span> Tel: <span data-field="combat_nbc_phone">{{ combat_nbc_phone }}</span> SA Chapter Email: <span data-field="combat_nbc_email">{{ combat_nbc_email }}</span>
  </p>
  <p>Scholarship Amount: <span data-field="combat_nbc_amount">{{ combat_nbc_amount }}</span> Veterans pursuing a degree related to the entertainment industry</p>

  <p><strong>Council of College and Military Educators Veteran Scholarship</strong> <a href="{{ ccme_url }}">{{ ccme_url }}</a></p>
  <p>
    <span data-field="ccme_address">{{ ccme_address }}</span> Tel: <span data-field="ccme_phone">{{ ccme_phone }}</span>
  </p>
  <p>Scholarship Amount: <span data-field="ccme_amount">{{ ccme_amount }}</span> Military Active Duty – Veterans</p>

  <p><strong>HotelPlanner.com Graduate Technology Scholarship for Military Families</strong> <a href="{{ hotelplanner_url }}">{{ hotelplanner_url }}</a></p>
  <p>
    Contact us at Scholarships (AT) hotelplanner.com
  </p>
  <p>Scholarship Amount: <span data-field="hotelplanner_amount">{{ hotelplanner_amount }}</span> (10 scholarships) Military Veterans – Spouses – Dependents</p>

  <p><strong>H&P Veterans Helping Veterans Scholarships</strong> <a href="{{ hp_veterans_url }}">{{ hp_veterans_url }}</a></p>
  <p>
    <span data-field="hp_veterans_address">{{ hp_veterans_address }}</span>
  </p>
  <p>Scholarship Amount: <span data-field="hp_veterans_amount">{{ hp_veterans_amount }}</span> (four) Military Scholarships for Student Veterans</p>
</div>